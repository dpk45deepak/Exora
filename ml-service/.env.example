# Copy to .env and fill in real values.

# Comma-separated list of allowed CORS origins (should be the Node gateway
# only — this service must never be reachable directly from a browser).
ALLOWED_ORIGIN="http://localhost:4000"
